import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.anlibenservicespro',
  appName: 'Meus Clientes',
  webDir: 'www/dasdas/',
  bundledWebRuntime: false
};

export default config;
